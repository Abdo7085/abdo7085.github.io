{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "Static Site (localhost:8000)",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-c", "import http.server,os;http.server.test(port=int(os.environ.get('PORT',8000)))"],
      "port": 8000,
      "autoPort": true
    }
  ],
  "autoVerify": false
}
